// TODO: Add namespace
enum Mode:byte { Read, Write }

enum ConnectionType:byte { Memory, I2C, UART, SPI }

table SettingList
{
    settings:[Setting];
}

table Setting
{
    mode:Mode;
    destination:string;
    connectionType:ConnectionType;
    payload:[ubyte];
}

root_type SettingList;
